# Toronto bookstore venue DENYLIST
# Used by scrapers/eventbrite_filtered.py to filter publisher Eventbrite feeds.
#
# Rule: events at venues matching any keyword below are EXCLUDED.
# Everything else from the publisher feed is treated as a plausible candidate.
#
# Format: one keyword per line, substring match (case insensitive).
# Comments after `#` are stripped. Blank lines are ignored.

# === Indigo / Chapters chain ===
# Indigo Books & Music is the big-box Canadian chain (Indigo, Chapters, Coles).
# We exclude their stores because the calendar focuses on independent venues.
Indigo
Chapters Indigo
IndigoSpirit
