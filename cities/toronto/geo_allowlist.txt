# Toronto geo allowlist for venue filtering
#
# Used by scrapers/eventbrite_filtered.py: events whose LOCATION contains
# at least one of these strings pass the geo check. Events with no location
# at all also pass (treated as online/virtual events from a Toronto org).
#
# Scope: City of Toronto (the amalgamated city = old Toronto + 5 inner boroughs).
# Does NOT include GTA satellite cities (Mississauga, Brampton, Markham, etc.)
# — those are separate municipalities.

Toronto
North York
Scarborough
Etobicoke
East York
